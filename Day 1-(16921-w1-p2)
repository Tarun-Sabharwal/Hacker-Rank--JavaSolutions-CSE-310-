// Write a program to take the 2 input of double type from the user. Display the values
// in separated by symbol ‘-’ if the values are positive otherwise display the message “Invalid Input”.

// Input Format
// Your program should take at least 2 inputs of double types.

// Constraints
// Input should be positive values

// Output Format
// Output should be the input values separated by ‘-‘ or “Invalid Input” message if any input is negative.


// code starts from here.

import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;
public class Main{
public static void main(String args[]) {
    Scanner sc =new Scanner(System.in);
    double x,y;
    x=sc.nextDouble();
    y=sc.nextDouble();
    if(x>0 && y>0)
        System.out.println(x+"-"+y);
    else
        System.out.println("Invalid Input");
        
}
}
