/*  Kavita and Mehar are playing a game. Kavita provides few numbers to Mehar and ask to find second smallest number from them. Write a program to help Mehar.

Input Format
Enter the size of array i.e. N
Enter N elements of array

Constraints
N should not be greater than 10

Output Format
An integer value   */

// code starts from here

import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
    Scanner sc=new Scanner(System.in);
    int n=sc.nextInt();
        if(n>10)System.out.print("Invalid");
        else{
        int arr[]=new int[n];
        for(int i=0;i<n;i++){
            arr[i]=sc.nextInt();
            }
        Arrays.sort(arr);
        System.out.print(arr[1]);
        }
    }
}

